# Design Lock — index (Deep Seabed Mining Law Corpus)

These six documents are the **constitution** of this corpus. They are **locked**: change them only by
a dated decision note in `judgement-calls.md`, never silently. Everything downstream — scripts,
schemas, folder layout, the site — serves these decisions.

The one principle everything serves: **provenance and version integrity override convenience, always.**

This corpus is a sibling of the Space Law Corpus and inherits its architecture. Only the sources,
concepts, languages, and maintenance cadence are domain-specific. Where a decision is a re-use of the
Space Law decision, it is marked *(inherited)*; where DSM differs, it is marked *(DSM-specific)*.

1. `01-provenance-schema.md` — metadata every authoritative document must carry.
2. `02-versioning-scheme.md` — identifiers, and how change is recorded (append-only).
3. `03-authoritative-text-policy.md` — what counts as authoritative; authentic languages; fidelity flags.
4. `04-licensing-policy.md` — rights to store/redistribute each source type.
5. `05-scope-boundary.md` — what is in and out of scope for deep seabed mining law.
6. `06-two-layer-separation.md` — the wall between authoritative and derived.

**Judgement calls** are recorded in `judgement-calls.md` (a "JC" each: options, choice, reason, date).

Locked: 2026-07-03.
