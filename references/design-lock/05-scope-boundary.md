# 05 — Scope boundary (TEMPLATE)

Decide deliberately what belongs in the corpus. Breadth is worthless if any record's provenance
is doubtful, so grow **instrument by instrument**, not by sweep.

- **In scope:** [list the instrument types for DOMAIN — e.g. primary statutes, regulations,
  binding guidance, key soft law].
- **Out of scope (until a dated decision):** [adjacent-but-different bodies of law; commentary;
  case law if not intended; policy that is not law].
- **Borderline items** go to `queue/candidates.md` for a recorded decision — not straight into the
  corpus.

Record every scope change as a dated decision note.
